All thumbnail images and videos have an alt tag that describes it as the property title's thumbnail.

We ensure all font has colour that contrasts hightly from their background.

All form submission is only on a submission button click. 

All input fields have a label to that corresponds/describe the input fields and some input fields that require additional information has placeholder text, for example, the date inputs.

We attempted to make the error messages as helpful as possible.